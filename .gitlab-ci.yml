image: python:3.13

stages:
  - build
  - lint
  - test

before_script:
    - apt-get update --yes
    - apt-get install --yes cmake
    - apt-get install --yes libblas3 liblapack3 liblapack-dev libblas-dev
    - pip install virtualenv
    - python -m venv .venv
    - source .venv/bin/activate

cache:
  key: pip-global-cache
  paths:
    - $CI_PROJECT_DIR/.cache/pip
    
build:
  stage: build
  rules:
  - if: '$CI_COMMIT_BRANCH == "docs"'
    when: never
  - when: always

  script:
    - export CMAKE_BUILD_PARALLEL_LEVEL=4
    - export PARALLEL_BUILD_TTNPP=8
    - python -m pip -v install .

  artifacts:
    paths:
        - /builds/quantum-software/pyttn/pyttn/
lint:
  stage: lint
  rules:
  - if: '$CI_COMMIT_BRANCH == "docs"'
    when: never
  - when: always

  script:
    - export SKIP_BUILD_TTNPP_LIBRARY=True
    - python -m pip -v install .[lint]
    - ruff check

test:
  stage: test
  rules:
  - if: '$CI_COMMIT_BRANCH == "docs"'
    when: never
  - when: always

  script:
    - export SKIP_BUILD_TTNPP_LIBRARY=True
    - python -m pip -v install .[test]
    - python -m pytest

pages:
  stage: pages
    - echo $CI_PAGES_URL
    - export SKIP_BUILD_TTNPP_LIBRARY=True
    - python -m pip -v install .[docs]
    - cd docs
    - make html
    - cp -r html ../public
    - cd ../
    - echo $CI_PAGES_URL
  artifacts:
    paths:
    - public

