image: python:latest

stages:
  - test
  #- deploy

test:
  stage: test
  script:
  - python -m pip install ./
  - pytest
  rules:
    - if: $CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH
    
#deploy-pages:
#  stage: deploy
#  script:
#  - mkdir .public
#  - cp -r docs/build/html/* .public
#  artifacts:
#    paths:
#    - public
#  rules:
#    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

